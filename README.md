# thehive-alert-import-api
This script allow to  import  alerts into TheHive via its REST API, helping when system resources are limited and both SIEM and SOAR platforms cannot run simultaneously. It solves RAM consumption constraints by allowing external alert processing and structured incident creation in TheHive without requiring direct parallel access to the SIEM server.
